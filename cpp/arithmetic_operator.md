# Arithmetic Operator 중 헷깔렸던 부분

## unary minus
- `-x`는 unary minus
- `- x`도 컴파일은 되지만 가독성을 위해 `-x` 권장

## 음수 정수 나눗셈 / 나머지
- C++11 이후 정수 나눗셈은 0 방향으로 절삭
- `-5 / 2` → `-2`
- 나머지 `%`의 부호는 왼쪽 피연산자를 따름
- `-5 % 2` → `-1`
